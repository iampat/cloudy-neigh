{
  "permissions": {
    "allow": [
      "view_file(*)",
      "write_to_file(*)",
      "replace_file_content(*)",
      "list_dir(*)",
      "grep_search(*)",
      "command(bazel build*)",
      "command(bazel query*)",
      "command(bazel cquery*)",
      "command(bazel aquery*)",
      "command(bazel mod*)",
      "command(bazel info*)",
      "command(bazel fetch*)",
      "command(bazel version*)",
      "command(bazel clean*)",
      "command(bazel test*)",
      "command(bazel run //:gazelle*)",
      "command(bazel run //:format*)",
      "command(bazel run //:buildifier*)",
      "command(gh pr view*)",
      "command(gh pr list*)",
      "command(gh pr diff*)",
      "command(gh pr checks*)",
      "command(gh pr create*)",
      "command(git status*)",
      "command(git diff*)",
      "command(git log*)",
      "command(git checkout*)",
      "command(git switch*)",
      "command(git branch*)",
      "command(git add*)",
      "command(git commit*)",
      "command(git push*)"
    ]
  }
}
